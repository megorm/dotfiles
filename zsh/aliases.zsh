

alias tmux="tmux -2"

# PS
alias psa="ps aux"
alias psg="ps aux | grep "
alias psr='ps aux | grep ruby'

# human readable units
alias du='du -h -d 2'

# Common
alias tf='tail -f'
alias cl='clear'
alias mkdir='mkdir -p'

